
Requirements:

* qt4-lib
* qt4-dev
* qt4-dev-tools (qmake)

To install just follow:

$ ./configure
$ make

Obs:
"make install" is not suported yet, so, your binary file will be generated in
the current dir.

To run invoke:

$ ./simvida
